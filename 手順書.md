# Performance Counter収集・分析手順書 Ver.0.2

## 1. 目的

ULAgent総合試験に向けて、CTC側のシステム全体および `ULAgent.Sender` のリソース使用状況を収集・分析する。

本手順書では以下を実施する。

- 実行環境と利用可能カウンターの自動確認
- Performance Counter収集
- BLG原本保存
- CSV変換
- Power Query分析
- 試験記録作成

---

## 2. 前提条件

- Windows環境
- Windows PowerShell 5.1が利用可能
- CTC側で `ULAgent.Sender` サービスが起動している
- ExcelおよびPower Queryが利用可能（分析時のみ）

管理者権限はPerformance Counter収集には原則不要。ULAgentサービスの登録・起動操作には管理者権限が必要。

### フォルダ構成

```text
PowerShell
├─ config
│  ├─ counters.txt
│  └─ counters.effective.txt       … Setup実行時に自動生成
├─ logs
├─ output
├─ scripts
│  ├─ Setup-PerformanceCollection.ps1
│  ├─ Start-PerformanceCollection.ps1
│  ├─ Invoke-PerformanceCollector.ps1
│  ├─ Stop-PerformanceCollection.ps1
│  └─ Convert-PerformanceLog.ps1
├─ CounterList_Ver0.1.md
├─ TestExecutionRecord_Ver0.1.md
└─ Day3_FileCopyAnalysis.xlsx
```

---

## 3. 収集対象

### システム全体

```text
\Processor(_Total)\% Processor Time
\Memory\Available Bytes
```

### Disk

```text
\PhysicalDisk(_Total)\Disk Read Bytes/sec
\PhysicalDisk(_Total)\Disk Write Bytes/sec
\PhysicalDisk(_Total)\Disk Transfers/sec
\PhysicalDisk(_Total)\Avg. Disk Queue Length
```

### Network

```text
\Network Interface(<対象NIC>)\Bytes Received/sec
\Network Interface(<対象NIC>)\Bytes Sent/sec
```

`Setup-PerformanceCollection.ps1` が実行PCの利用可能なNICを自動検出する。

### Process

既定の対象はCTC側の `ULAgent.Sender`。

```text
\Process(ULAgent.Sender)\% Processor Time
\Process(ULAgent.Sender)\Working Set - Private
\Process(ULAgent.Sender)\Private Bytes
\Process(ULAgent.Sender)\IO Read Bytes/sec
\Process(ULAgent.Sender)\IO Write Bytes/sec
\Process(ULAgent.Sender)\ID Process
```

---

## 4. 環境準備

PowerShellフォルダで次を実行する。

```powershell
& '.\scripts\Setup-PerformanceCollection.ps1'
```

この処理で以下を自動実施する。

- `output`、`logs`、`config` フォルダの作成
- 必要なPowerShellコマンドと実行条件の確認
- PC固有のNIC名の解決
- 利用できないカウンターの除外
- `config\counters.effective.txt` の生成

正常時は `Result : Ready` が表示される。`SkippedCount` が0以外の場合は、ULAgent.Senderサービスの起動状態と警告内容を確認する。

---

## 5. 収集開始

### 実行前確認

```text
ULAgent.Senderサービスが起動している
Setup-PerformanceCollection.ps1が正常終了する
outputおよびlogsへ書き込める
同じTestIDの既存出力と重複しない
```

### 実行

```powershell
& '.\scripts\Start-PerformanceCollection.ps1' -TestId '<TestID>'
```

例：

```powershell
& '.\scripts\Start-PerformanceCollection.ps1' -TestId 'DAY3-FILECOPY-001'
```

StartスクリプトはSetupも自動実行するため、環境準備の再確認を兼ねる。

正常時は次の項目が表示される。

```text
Result         : Started
TestId         : <TestID>
ProcessId      : <収集プロセスID>
CounterCount   : <このPCで有効なカウンター数>
SkippedCount   : <除外されたカウンター数>
BlgPath        : <出力先>
```

---

## 6. 試験実施

```text
収集開始
  ↓
事前待機（5秒程度）
  ↓
ULAgent配下へ試験ファイルを投入
  ↓
転送完了を確認
  ↓
事後待機（5秒程度）
  ↓
収集停止
```

ULAgent.Senderの既定スキャン間隔は60秒。ファイル投入後は固定時間だけで判断せず、次のいずれかで転送完了を確認する。

```powershell
Invoke-RestMethod 'http://127.0.0.1:5080/api/status' | ConvertTo-Json
```

または、Senderログ、転送元ファイルの削除、Receiver側の受信状態を確認する。

---

## 7. 収集停止

開始時と同じPowerShell画面でなくても実行できる。

```powershell
& '.\scripts\Stop-PerformanceCollection.ps1'
```

正常時は次の項目が表示される。

```text
Result
SampleCount
CounterCount
BlgPath
CsvPath
DataRows
ColumnCount
```

停止時にBLGからCSVへの変換も自動実施する。

---

## 8. 結果確認

確認項目：

```text
SampleCount > 0
CounterCountが開始時の表示値と一致する
BLGファイルが存在する
CSVファイルが存在する
DataRowsがSampleCountと一致する
ColumnCountがCounterCount + 1（Timestamp列）と一致する
```

NICが複数あるPCや利用できないカウンターがあるPCでは、CounterCountは14固定にならない。

必要時の手動変換：

```powershell
& '.\scripts\Convert-PerformanceLog.ps1' -BlgPath '<BLGファイル>'
```

---

## 9. Power Query更新

分析ブック：

```text
Day3_FileCopyAnalysis.xlsx
```

更新手順：

```text
Excel起動
 ↓
データ
 ↓
クエリと接続
 ↓
接続先CSV変更
 ↓
すべて更新
```

CSV列数はPC上で有効になったカウンター数により変わる可能性がある。列名とデータ行数を確認する。

---

## 10. 試験記録

記録先：

```text
TestExecutionRecord_Ver0.1.md
```

記録項目：

```text
TestID
収集開始日時
収集終了日時
対象PC
対象プロセス（ULAgent.Sender）
対象NIC
BLG、CSV、ログのパス
SampleCount
CounterCount
SkippedCount
DataRows
ColumnCount
結果
備考
```

---

## 11. 異常時確認

### カウンター取得失敗・SkippedCount発生

```text
ULAgent.Senderサービスが起動しているか
counters.txtの対象プロセス名が正しいか
対象NICが有効か
警告に表示されたカウンターが対象PCに存在するか
```

### 収集開始済みエラー

`logs\collection-state.json` が存在し、記録された収集プロセスが稼働している場合は、先にStopスクリプトを実行する。

### BLG未作成

```text
outputフォルダへの書込み権限
logs内のcollector-*.stderr.log
Windows PowerShell 5.1の存在
```

### 欠損値

初回サンプル、欠損カウンター、発生時刻を確認する。欠損値は0へ置換しない。

---

## 12. 成果物

```text
config\counters.txt
config\counters.effective.txt
scripts\Setup-PerformanceCollection.ps1
scripts\Start-PerformanceCollection.ps1
scripts\Invoke-PerformanceCollector.ps1
scripts\Stop-PerformanceCollection.ps1
scripts\Convert-PerformanceLog.ps1
output\*.blg
output\*.csv
logs\*.log
Day3_FileCopyAnalysis.xlsx
CounterList_Ver0.1.md
TestExecutionRecord_Ver0.1.md
```

---

## 13. 補足

- `counters.txt` は編集可能な原設定。
- `counters.effective.txt` はSetup実行時にPC環境から自動生成されるため、直接編集しない。
- Receiver側を測定する場合は、対象PC上でプロセス名を `ULAgent.Receiver` に変更した設定が必要。
- 外部APIや有料サービスは使用しない。

---

## 14. ULAgent.SenderがないPCでの自己診断

実機サービスがないPCでは、Windows標準機能だけで一時的な `ULAgent.Sender.exe` 模擬プロセスを作成し、収集からCSV変換まで確認できる。

```powershell
& '.\scripts\Test-PerformanceCollection.ps1'
```

正常時は `Result : Passed` と、サンプル数・カウンター数・CSV行数が表示される。模擬プロセス、コンパイルした実行ファイル、BLG、CSV、ログはテスト後に自動削除される。

結果ファイルを確認用に残す場合：

```powershell
& '.\scripts\Test-PerformanceCollection.ps1' -KeepArtifacts
```

安全上、実際の `ULAgent.Sender` がすでに動作しているPCでは自己診断を開始しない。
